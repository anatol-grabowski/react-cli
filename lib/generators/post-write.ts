/**
 * For each option, define a function to handle it.
 * example: 'useSemiColon' has a function that writes semicolons to file
 */
import { GeneratorFunction } from ".";
 /**
  * 
  */
 
 export function handleUseSemiColons(code: 'string') {
     // generate the file, then append semicolons where they need to be.
     // probably possible with esprima.
     // how to add strings given a Ln number and Col number?
     return String()
 }