/**
 * How do we set up tests?
 * + Circle CI
 *  + Create dummy files && do conversions && query results
 *  + Create templates && query results
 */

// templates


import { updateComponent, updateIndex, updateStyle } from '../lib/replacers'

 describe('Converting Components',() => {
    test('Convert from Function to Class',() => {
        // updateComponent()
    })
 })